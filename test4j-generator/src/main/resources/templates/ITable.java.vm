package ${basePackage};

import ${basePackage}.mapping.*;

/**
 *
 * @author: generate code
 */
public interface ITable {
    #foreach(${obj} in ${objs})

    String t_${obj.tableName} = ${obj.mappingName}.Table_Name;
    #end
}