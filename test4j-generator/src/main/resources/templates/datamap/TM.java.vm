package ${basePackage}.datamap;

import ${basePackage}.datamap.table.*;

/**
 * Table Data Map
 */
public interface TM {
#foreach(${config} in ${configs})

    ${config.tableMap.name}.Factory ${config.table_no_prefix} = new ${config.tableMap.name}.Factory();
#end
}